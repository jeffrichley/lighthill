# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tooling caches + coverage artifacts
.ruff_cache/
.mypy_cache/
.pytest_cache/
.hypothesis/
.coverage
coverage.xml

# subagent-driven-development scratch (briefs, reports, diffs, progress ledger)
.superpowers/

# regenerable sim_validation run dumps + MuJoCo's auto-written log (never tracked)
_armswing_run.json
_*_run.json
MUJOCO_LOG.TXT
